# Собранные бинарники
/kino
/bin/

# Локальная база
*.db
*.db-wal
*.db-shm

# Ключи. config.toml живёт в ~/.config/kino, но копию легко положить рядом —
# и она не должна попасть в историю ни при каких обстоятельствах.
.env
config.env
config.toml

.DS_Store
